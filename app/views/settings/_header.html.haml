#nav
  %ul
    %li{:class => params[:controller] == 'settings/users' && 'active'}
      = link_to t('panua.words.users'), settings_edit_user_path
    %li{:class => params[:controller] == 'settings/tags' && 'active'}
      = link_to t('panua.words.tags'), settings_tags_path
    %li{:class => params[:controller] == 'settings/services' && 'active'}
      = link_to t('panua.settings.services.index'), settings_services_path
